<h1><%= @task.title %></h1>
<p><%= @task.details %></p>



<p><%= @task.completed === true ? "Task completed 😊 " : "Task not completed 🤬" %></p>

<p><%= link_to "Back to index", tasks_path %></p>
<p><%= link_to "Edit your task", edit_task_path %></p>

<%= link_to "Delete this task", task_path(@task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
